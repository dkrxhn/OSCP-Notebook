Places to check for File Read Vuln on windows:
```
C:\Windows\win.ini
```
- `/etc/passwd` for linux
# Find creds
if port 80 is open:
```
C:\inetpub\wwwroot\application\web.config
```
- might also be common name from nmap:
```
C:\inetpub\wwwroot\hacksmartersec\web.config
```
